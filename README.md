# ML Model Validation Framework

A framework to auto-validate machine learning models based on predefined rules.
Includes:
- training-validation consistency checks
- feature drift detection
- performance metric validation

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the notebook or `python model_validation.py`

## Tech Used
Python, Scikit-learn, Pandas, NumPy
